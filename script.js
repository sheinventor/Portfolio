function searchFunction() {
    let query = document.getElementById("search").value;
    if (query) {
        alert("Buscando: " + query);
        // Aquí puedes redirigir a una página de resultados o filtrar contenido dinámicamente
    }
}
